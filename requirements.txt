# ============================================================
# requirements.txt — Dépendances du projet BE2_Data_science_ECL
# Ce fichier permet d’installer tout ce qui est nécessaire pour
# exécuter le script (sparse TF-IDF, embeddings, graphe, LDA/UMAP,
# visualisations, etc.).
# ============================================================

# Stack scientifique de base
numpy==1.26.4
scipy==1.11.4
pandas==2.2.2
matplotlib==3.8.4

# Machine Learning / NLP
scikit-learn==1.4.2
sentence-transformers==2.7.0

# Graphes (citations)
networkx==3.2.1

# LDA / Topic Modeling (Section 6)
gensim==4.3.2
umap-learn==0.5.6
umap-plot==0.5.5
pyLDAvis==3.4.1

# Barre de progression (utilisée dans les embeddings enrichis par le graphe)
tqdm==4.66.4

# Affichage dans Spyder / notebooks (display_side_by_side)
ipython==8.26.0
